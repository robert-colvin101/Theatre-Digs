<h2><%= @property.name %></h2>
<p><%= @property.address %></p>
<p><%= @property.price %></p>
<p><%= @property.city %></p>
<%= link_to "Delete #{@property.name}",
            property_path(@property),
            method: :delete,
            data: {confirm: "Are you sure?"} %>
<br>
<%= link_to "See other properties",
            properties_path %>
